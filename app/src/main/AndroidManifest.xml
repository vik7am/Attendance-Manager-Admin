<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vikrant.attendancemanageradmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".admin.StudentActivity" />
        <activity android:name=".admin.AdminMainActivity" />
        <activity android:name=".admin.TeacherActivity" />
        <activity android:name=".admin.SubjectActivity" />
        <activity android:name=".admin.TimeTableSettingActivity" />
        <activity android:name=".admin.TimeTableActivity" />
        <activity android:name=".teacher.TeacherMainActivity" />
        <activity android:name=".teacher.TeacherTimeTableActivity" />
        <activity android:name=".teacher.TeacherAttendanceActivity" />
        <activity android:name=".teacher.MarkAttendanceActivity"></activity>
    </application>

</manifest>